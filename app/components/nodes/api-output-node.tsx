export function ApiOutputNodeBody(_props: { id: number }) {
    return (
        <p className="text-sm text-muted-foreground">
            API service responses will use the image state at this point. Batch pipeline mode passes this node through.
        </p>
    )
}
